import { StateData } from "./GameController";

export class NarrativeGenerator {
  generate(state: StateData): string {
    // Interact with GPT model here
    // Placeholder implementation
    return `Narrative based on state: ${JSON.stringify(state)}`;
  }
}
